apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpuid
  namespace: gpuid
  labels:
    app: gpuid
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gpuid
  template:
    metadata:
      labels:
        app: gpuid
    spec:
      serviceAccountName: gpuid
      containers:
        - name: gpuid
          image: ghcr.io/mchmarny/gpuid@sha256:0363f051d799ec629e0cf59c3c0d14a1e5a8bfb283f38b99e2e311e763547f8e
          resources:
            requests:
              cpu: "500m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8080
              name: metrics
          livenessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
      restartPolicy: Always

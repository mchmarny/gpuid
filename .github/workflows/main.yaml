name: main

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:

  test:
    env:
      GO_VERSION: "1.25.0"
      ERR_VULNERABILITY_SEV: "CRITICAL,HIGH,MEDIUM"
      LINTER_VERSION: "v2.4.0"
      LINTER_GO_CONFIG_PATH: ".golangci.yaml"
      LINTER_YAML_CONFIG_PATH: ".yamllint"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      id-token: write
    steps:
    - uses: mchmarny/common/go-test@4df2bd275f9a4569e7ceadc31b67b043118256ff  # v0.2.0
      with:
        go-version: ${{ env.GO_VERSION }}
        scan-severity: ${{ env.ERR_VULNERABILITY_SEV }}
        linter-version: ${{ env.LINTER_VERSION }}

---
name: 'Go Lint'
description: 'Run go vet, golangci-lint, and yamllint with pinned versions from .settings.yaml'

inputs:
  go_version:
    description: 'Go version to install'
    required: true
  golangci_lint_version:
    description: 'golangci-lint version (e.g. v2.4.0)'
    required: true
  lint_timeout:
    description: 'Lint timeout duration (e.g. 5m)'
    required: false
    default: '5m'

runs:
  using: 'composite'
  steps:
    - name: Setup Go
      uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c  # v6.4.0
      with:
        go-version: ${{ inputs.go_version }}
        cache: true

    - name: Run go vet
      shell: bash
      run: go vet ./...

    - name: Run golangci-lint
      if: hashFiles('.golangci.yaml') != ''
      uses: golangci/golangci-lint-action@82606bf257cbaff209d206a39f5134f0cfbfd2ee  # v9.2.1
      with:
        version: ${{ inputs.golangci_lint_version }}
        args: --timeout=${{ inputs.lint_timeout }}

    - name: Lint YAML
      if: hashFiles('.yamllint') != ''
      uses: karancode/yamllint-github-action@4052d365f09b8d34eb552c363d1141fd60e2aeb2  # v3.0.0
      with:
        yamllint_config_filepath: .yamllint
